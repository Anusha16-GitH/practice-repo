name: GitHub Practice Workflow

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show success message
        run: echo "Workflow is running successfully"
